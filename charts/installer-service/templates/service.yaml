apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name:  installer-service
  name: installer-service
  namespace: openfuyao-system
spec:
  type: ClusterIP
  ports:
    - name: https
      port: 9210
      targetPort: https
  selector:
    app.kubernetes.io/name: installer-service